# Jankurai badge auto-update configuration.
# When present, `jankurai audit` will refresh badge artifacts after writing
# score reports. Set `enabled = false` to disable.

enabled = true
score = "agent/baselines/main.repo-score.json"
svg = "agent/jankurai-badge.svg"
json = "agent/jankurai-badge.json"
readme = "README.md"
link = "agent/baselines/main.repo-score.json"
update_readme = true
label = "jankurai"
